{
  "name": "@local/root",
  "description": "Web components (Firebase login, ...)",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/akauppi/aside-keys.git"
  },
  "type": "module",
  "private": true,
  "engines": {
    "node": ">= 13.2"
  },
  "--": "-- following stuff matters for development, only --",
  "scripts": {
    "build": "npm run --silent _build-all && vite build",
    "_build-all": "(cd packages/aside-keys && npm run build)",
    "--": "",
    "dev": "concurrently -k -n aside-keys,vite \"npm run --silent _watch1\" \"vite --port ${PORT:-5000} --clearScreen=false\"",
    "_watch1": "(cd packages/aside-keys && npm run watch)"
  },
  "dependencies": {
    "@local/aside-keys": "file:packages/aside-keys",
    "firebase": "^8.2.5"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^11.1.1",
    "concurrently": "^5.3.0",
    "rollup": "^2.38.4",
    "rollup-plugin-css-only": "^3.1.0",
    "rollup-plugin-livereload": "^2.0.0",
    "rollup-plugin-svelte": "^7.1.0",
    "rollup-plugin-terser": "^7.0.2",
    "svelte": "^3.32.1",
    "vite": "^2.0.0-beta.64"
  }
}
